# Local integration-test database. Never point this at anything with real data —
# the suites truncate every table between tests.
TEST_DATABASE_URL="postgresql://postgres@localhost:5433/evotasks_test?host=/tmp"
